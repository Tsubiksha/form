import {render,screen} from '@testing-library/react';import {MemoryRouter,Route,Routes} from 'react-router-dom';import {beforeEach,expect,test,vi} from 'vitest';import API from '../services/api';import Dashboard from '../pages/Dashboard';import FormBuilder from '../pages/FormBuilder';import {ToastProvider} from '../components/ToastProvider';
vi.mock('../services/api',()=>({default:{get:vi.fn(),post:vi.fn(),patch:vi.fn(),delete:vi.fn()}}));
beforeEach(()=>vi.clearAllMocks());
test('dashboard renders owned forms',async()=>{API.get.mockResolvedValue({data:{total_forms:1,published_forms:0,draft_forms:1,archived_forms:0,total_responses:0,recent_forms:[{id:1,title:'Customer form',description:'Test',status:'draft'}]}});render(<MemoryRouter><Dashboard/></MemoryRouter>);expect(await screen.findByText('Customer form')).toBeInTheDocument()});
test('builder loads existing fields',async()=>{API.get.mockImplementation(url=>Promise.resolve({data:url.endsWith('/versions')?[]:{id:1,title:'Customer form',description:'Test',status:'draft',fields:[{id:2,label:'Email',field_type:'email',required:true,options:[],validation_rules:[]}]}}));render(<ToastProvider><MemoryRouter initialEntries={['/forms/1/builder']}><Routes><Route path="/forms/:formId/builder" element={<FormBuilder/>}/></Routes></MemoryRouter></ToastProvider>);expect(await screen.findByRole('heading',{name:'Customer form'})).toBeInTheDocument();expect(screen.getByRole('heading',{name:/Email/})).toBeInTheDocument()});
